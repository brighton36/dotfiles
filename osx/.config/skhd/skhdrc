#  NOTE(koekeishiya): A list of all built-in modifier and literal keywords can
#                     be found at https://github.com/koekeishiya/skhd/issues/1

# Change Focus
cmd - 0x1B : /opt/homebrew/bin/yabai -m window --focus prev # [
cmd - 0x18 : /opt/homebrew/bin/yabai -m window --focus next # ]

# Swap position
alt + cmd - 0x1B : /opt/homebrew/bin/yabai -m window --swap prev # [
alt + cmd - 0x18 : /opt/homebrew/bin/yabai -m window --swap next # ]

# Toggle float
cmd - d : /opt/homebrew/bin/yabai -m window --toggle float --grid 4:4:1:1:2:2

# Switch workspaces
# Disabled here, because we need System Integrity protection installed. I guess
# See the karabiner settings for this. TODO: add cmd1-9

# window size horizontal
cmd - h : /opt/homebrew/bin/yabai -m window --resize left:-20:0
cmd - l : /opt/homebrew/bin/yabai -m window --resize right:20:0

# window size vertical
cmd - j : /opt/homebrew/bin/yabai -m window --resize bottom:0:-20
cmd - k : /opt/homebrew/bin/yabai -m window --resize top:0:20

# toggle window split type
cmd - space : /opt/homebrew/bin/yabai -m window --toggle split

# balance size of windows
shift + cmd - space : /opt/homebrew/bin/yabai -m space --balance


# TODO cmd -e : emacs
# TODO: cmd - esc: lock screen
# TODO: cmd - enter:
# TODO cmp -p : /Applications/Firefox.app/Contents/MacOS/firefox
cmd  - return : /Applications/iTerm.app/Contents/MacOS/iTerm2
